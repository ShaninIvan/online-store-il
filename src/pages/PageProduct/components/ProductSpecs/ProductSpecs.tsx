import React from 'react'
import styles from './ProductSpecs.module.less'

type PropsType = {
   
}

export const ProductSpecs: React.FC<PropsType> = ({}) => {

    return (
        <div className={styles.productspecs}>
            
        </div>
    )
}