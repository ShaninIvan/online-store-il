import React from 'react'
import styles from './Specs.module.less'

type PropsType = {
   
}

export const PageProductSpecs: React.FC<PropsType> = ({}) => {

    return (
        <div className={styles.specs}>
            
        </div>
    )
}