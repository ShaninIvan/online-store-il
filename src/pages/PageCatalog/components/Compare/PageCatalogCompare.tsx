import React from 'react'
import styles from './Compare.module.less'

export const PageCatalogCompare: React.FC = () => {
    return (
        <div className={styles.compare}>
            <h4>Compare Products</h4>
            You have no items to compare.
        </div>
    )
}
