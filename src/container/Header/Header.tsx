import React from 'react'
import styles from './Header.module.less'

/**
 * Root component of top of application
 */
export const Header: React.FC = () => {
    return <header className={styles.header}>header</header>
}
